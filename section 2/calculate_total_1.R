# You can do arithmetic calculations on vectors.

# It is important to know that if you sum two vectors in R, 
# it takes the element-wise sum. For example, the following 
# three statements are completely equivalent:
  
c(1, 2, 3) + c(4, 5, 6)
c(1 + 4, 2 + 5, 3 + 6)
c(5, 7, 9)