# Declare variables of different types
my_numeric <- 42
my_character <- "universe"
my_logical <- FALSE 

# You can use "typeof()" or "class()"
typeof(my_numeric)
class(my_numeric)

typeof(my_character)
class(my_character)

typeof(my_logical)
class(my_logical)
